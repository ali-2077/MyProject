how to upload file into github


git add file.txt
#  Add them to your local Git repository so they’ll be tracked by Git.


git commit -m "Add file.txt."
#commit changes


git push origin master
#Uploading the project into github in "master", branch.

